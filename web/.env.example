# Raceway Web UI Configuration
# This should match the server configuration in raceway.toml

# API Base URL - where the Raceway server is running
VITE_API_BASE_URL=http://localhost:8080

# API Key for authentication (optional)
# Set this if your server has auth_enabled = true
# Note: In dev mode, set this as RACEWAY_KEY environment variable when running npm run dev
# Example: RACEWAY_KEY=joe npm run dev
RACEWAY_KEY=
